services:
  - type: web
    name: planning-poker
    env: node
    buildCommand: npm install && npm install --prefix client && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: CLIENT_ORIGIN
        value: https://planning-poker.onrender.com  # update to your actual URL
